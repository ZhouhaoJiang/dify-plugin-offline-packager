[project]
name = "dify-plugin-offline-packager"
version = "0.1.0"
description = "Build and verify Dify 3.12 Python plugins for offline installation"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
